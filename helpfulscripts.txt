#To enter the virtual environment
source borehamwood-xwp-003-juniper-env/bin/activate

#This needs to be run once to install all the relevant python dependencies  (should work but 
pip install -r helfulrequirements.txt   (installing ansible should also install ansible galaxy)

#Note that in pkgs.sh,  epel-release is from centos, so can be ignored and replace the yum package manager with the apt 

#To leave the virtual environment
deactivate 



